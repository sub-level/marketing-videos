# public/sfx

One-shot sound effects (whoosh, whip, click, ding, page-turn) go here. Pull
from an attribution-free pack such as `@remotion/sfx` and mirror the files
locally so headless renders never depend on the network.
